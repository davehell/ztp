{block content}
<h1 n:inner-block="title">{if $export}Export{else}Seznam{/if} {if $protokol == 'testy'}testovacích{else}změnových{/if} protokolů</h1>

<ul>
  {foreach $vsechnyVerze as $v}
    <li>
      {if $export}
        <a n:href="Verze:export, 'id' => array($v->id), 'protokol' => $protokol" title="Export verze {$v->nazev}">verze {$v->nazev}</a>
      {elseif $protokol == 'testy'}
        <a n:href="Verze:zmeny, $v->id" title="Testovací protokol verze {$v->nazev}">verze {$v->nazev}</a>
      {else}
        <a n:href="Verze:zmeny, $v->id" title="Změnový protokol verze {$v->nazev}">verze {$v->nazev}</a>
      {/if}

      {if $v->datum}
        - {$v->datum|date:'%d.%m.%Y'}
      {/if}
    </li>
  {/foreach}
</ul>
